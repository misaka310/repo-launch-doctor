# Before/After cases

This directory intentionally contains no placeholder cases. The requested three cases must be actual changes to repositories controlled by the maintainer, with immutable before/after commits and generated reports. None are available in this repository's history yet, so publishing invented examples would violate the evidence policy. Add each case only after its source repository and reports are verifiable.
